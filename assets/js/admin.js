/**
 * Публичный локальный журнал заявок отключён.
 *
 * Реальная админ-панель должна работать только через backend с серверной
 * аутентификацией и общим хранилищем. Пароли и заявки не хранятся в браузере.
 */

(function () {
  "use strict";

  window.domianAdmin = Object.freeze({
    enabled: false
  });
})();
