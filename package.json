{
  "name": "domian-kvartal-static-site",
  "private": true,
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "test:browser": "playwright test",
    "seo:audit": "node _tools/seo-audit.mjs"
  },
  "devDependencies": {
    "@playwright/test": "1.62.0"
  }
}
